 <% if logged_in? %>
   <div class="row">
     <section class="user_info">
       <%= render 'shared/user_info' %>
     </section>
     <section class="training_form">
       <%= render "shared/training_form" %>
     </section>
   </div>
   <% else %>
   <% redirect_to root_path %>
   <% end %>